import '@testing-library/jest-dom';

// Polyfill fetch for tests (used by real-login e2e test)
import 'cross-fetch/polyfill';

// Optional: add other global test setup here (e.g., mocking next/router if needed)
